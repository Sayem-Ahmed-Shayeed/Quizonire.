import 'package:flutter/material.dart';

const deepBlue = Color(0xff071952); //used for background
const chromeWhite = Color(0xffE7F0DC); //used for appbarColor
const transparentWhite = Color.fromARGB(
    40, 255, 255, 255); // used for making the opacity of the image less
