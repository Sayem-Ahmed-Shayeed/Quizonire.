import 'package:flutter/material.dart';

const deepBlue = Color(0xff021526); //used for background
const chromeWhite = Color(0xff694F8E); //used for appbarColor
const transparentWhite = Color.fromARGB(
  40,
  255,
  255,
  255,
); // used for making the opacity of the image less
const buttonColor = Color(0xffF8EDED);
